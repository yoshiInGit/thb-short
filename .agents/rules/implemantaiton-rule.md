---
trigger: always_on
---

実装に当たって以下の方針に従って

# ファイル操作の箇所

パイプラインの要素であるアトミックな処理を実行する関数内（stages/\*）でのファイルの入出力操作は禁止です。
必ず、パイプラインやステージランナーなどの外部からファイルの入出力を行い、それぞれの関数に渡すようにして。

# 不要な例外処理を禁止

例外をキャッチして、エラーをprintするだけのような、意味のない、例外処理は禁止。
例外処理することによって、システムにポジティブな影響がある場合だけ行って。
